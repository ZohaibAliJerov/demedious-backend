import puppeteer from "puppeteer";
import {scroll} from "./pageScroll.js";

const stellenausschreibungen = async () =>{
          try {
              
        } catch (err) {
            console.error(err)
        }
    }